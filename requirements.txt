# Core pipeline
requests>=2.31.0
tqdm>=4.66.0
numpy>=1.24.0
scipy>=1.10.0
laspy[lazrs]>=2.5.0
pykdtree>=1.3.7  # optional accelerator; falls back to scipy if missing

# Visualization / notebooks
matplotlib>=3.7.0

# Forward model and ML experiments
torch>=2.1.0
